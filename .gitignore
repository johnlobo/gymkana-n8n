# Secretos - nunca deben subirse
secrets/
.env

# Datos internos de n8n (credenciales cifradas, ejecuciones, DB local)
n8n_data/

# Capturas de pantalla sueltas usadas durante el desarrollo
Captura de pantalla*.png

# Bytecode de Python
__pycache__/
*.pyc

# venv de Kokoro TTS (en disco, no en /tmp que es tmpfs/RAM)
.venv-kokoro/
.pip-tmp/
